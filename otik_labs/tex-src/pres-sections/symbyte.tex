
\subsection{О терминах}
\begin{frame}{\insertsubsection}
\begin{adjustwidth}{-4.5em}{-4.5em}
\small
\setlength{\leftmargini}{0em} 
\setlength{\parskip}{0.3\parskip} 

\termin{Символ} "--- элемент качественной информации $a \in A$ (множество $A$ "--- алфавит).

\termin{Текст} 
% (исходный (несжатый) текст, сжатый текст) 
"--- последовательность $m \in A^+$ таких элементов.

\vfill
% \vfill

На практике для всех алгоритмов, где алфавит может быть произвольным, 
для ЭВМ с~8-битным байтом
\mbox{\termin{символ кодирования = байт}} (%так как в~большинстве ЭВМ байт 8-битен "--- это %$0...255$
\hex{00...FF}), 
\mbox{\termin{исходный текст = любой бинарный файл,}} сжатый текст "--- тоже бинарный файл:
% \vfill
\begin{itemize}

% \item использование в~программе для ЭВМ символов меньших, чем байт "--- неудобно;
% \vfill
% \item использование символов 
% фиксированной разрядности
% б\'{о}льших, чем байт
% % "--- даёт 
% $\implies$
% слишком большой алфавит $\implies$ объёмные структуры данных для восстановления.

\item использование в~программе для ЭВМ символов, не кратных байту "--- неудобно;

\item использование символов 
% фиксированной разрядности 
в~два байта и~более
$\implies$
слишком большой алфавит.
% $\implies$ объёмные структуры данных для восстановления.

\vfill
% \end{itemize}
% 
% \begin{itemize}
% \item использование символов сложной структуры или алфавита типа «все байты, кроме \hex{00}, \hex{01} ...» не позволяет рассматривать в~качестве исходного текста произвольный файл и~при этом не даёт выигрыша в~объёме.

\item использование в~качестве символа кодирования печатного символа ASCII или koi8r/cp1251/dos/iso/maccyrillic 
% (любой печатный символ "--- байт, но не всякий байт "--- это печатный символ) 
не~позволяет рассматривать в~качестве исходного текста произвольный файл и~приводит к~труднодиагностируемым ошибкам;

\vfill


\item использование  печатного символа UTF-8 (144\,697 символов Unicode в~2023 г.)
% не позволяет рассматривать в~качестве исходного текста произвольный файл, приводит к~труднодиагностируемым ошибкам 
% и~при этом не даёт выигрыша в~объёме;
"--- \mbox{то же самое} + большой алфавит  + переменная длина = проигрыш в~объёме.

\end{itemize}
% % Ни для какого алгоритма н
% Не имеет смысла использовать в~качестве символа кодирования
% % «печатный символ ASCII», «печатный символ KOI-8/cp1251» или «печатный символ Unicode»!
% печатные символы любого вида!

% \textterminblue{Символ кодирования $=$ произвольный байт} $\neq$ печатный символ!
% 
% \textterminblue{Исходный текст $=$ произвольный файл} $\neq$ plain text
% 
% {Сжатый текст}  $\neq$ plain text


% Для алгоритма Хэмминга и~т.\,п. символ "--- отдельный бит. 

% \begin{enumerate}
% \item использование неполного байтового алфавита (печатные символы ASCII или KOI-8/cp1251) не даёт выигрыша по размеру служебных структур;
% \item использование алфавита из структур переменной длины  (печатные символы UTF-8 и)
% \end{enumerate}


% так как в~большинстве ЭВМ байт 8-битен "--- это \text{любое} безнаковое значение $0...255$:  


% В~примерах для символов используются обозначения 
% \vfill

В~книгах для наглядности используются обозначения $A, B, ...$ и т.\,п. (маленький алфавит + визуальное отличие символа от индекса или частоты),
но в~программе это всё равно байты!

\end{adjustwidth}

\end{frame}

